<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Essential Screen Blocking Cover Permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- Main Dashboard Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lockscreen PIN Verification Screen -->
        <activity
            android:name=".PinActivity"
            android:exported="false"
            android:label="@string/pin_setup_title"
            android:theme="@style/Theme.MyApplication" />

        <!-- Pomodoro Study Timer Screen -->
        <activity
            android:name=".TimerActivity"
            android:exported="false"
            android:label="@string/start_focus"
            android:theme="@style/Theme.MyApplication" />

        <!-- Overlay blocker warning service -->
        <service
            android:name=".OverlayService"
            android:exported="false" />

        <!-- Draggable study countdown balloon overlay service -->
        <service
            android:name=".FloatingBubbleService"
            android:exported="false" />

        <!-- Core blocking Accessibility Service -->
        <service
            android:name=".FocusAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Anti-Uninstall Device Administrator Policy Receiver -->
        <receiver
            android:name=".FocusDeviceAdminReceiver"
            android:label="FocusVault Uninstall Lock"
            android:description="@string/desc_app_blocker"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_rules" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>

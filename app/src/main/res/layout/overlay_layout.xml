<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:keepScreenOn="true"
    android:gravity="center"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_centerInParent="true">

        <!-- Glowing Warning Icon -->
        <TextView
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:text="🚨"
            android:textSize="48sp"
            android:gravity="center"
            android:background="@drawable/bg_square_icon"
            android:backgroundTint="@color/accent_red_glow"
            android:layout_marginBottom="24dp" />

        <!-- Warning Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/overlay_warning_title"
            android:textColor="@color/accent_red"
            android:textSize="26sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"
            android:fontFamily="sans-serif-black"
            android:gravity="center" />

        <!-- Subtitle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ADDICTION SHIELD ENGAGED"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:textStyle="bold"
            android:letterSpacing="0.15"
            android:layout_marginBottom="24dp"
            android:gravity="center" />

        <!-- Description Message Card -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="32dp"
            app:cardCornerRadius="14dp"
            app:cardBackgroundColor="@color/dark_gray"
            app:cardElevation="0dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/overlay_warning_msg"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:gravity="center"
                android:padding="20dp"
                android:lineSpacingMultiplier="1.2" />
        </androidx.cardview.widget.CardView>

        <!-- Back to Study Button -->
        <Button
            android:id="@+id/btn_overlay_back"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:text="@string/button_back_to_study"
            android:textSize="16sp"
            style="@style/FocusButton" />

    </LinearLayout>
</RelativeLayout>
